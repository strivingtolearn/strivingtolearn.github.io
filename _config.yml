markdown: kramdown
highlighter: pygments

kramdown:
    smart_quotes: ["apos", "apos", "quot", "quot"]

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "default"

# vim: set ts=2 sw=2 et:
